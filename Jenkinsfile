pipeline {
    agent any
    stages {
        stage('Stage1'){
           steps {
               sh '''
               ls -la
               sort -C numbers
               '''
           }
        }
    }
}
